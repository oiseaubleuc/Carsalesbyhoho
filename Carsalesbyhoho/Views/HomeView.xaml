<UserControl x:Class="Carsalesbyhoho.Views.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Carsalesbyhoho.Views"
             xmlns:vm="clr-namespace:Carsalesbyhoho.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <UserControl.DataContext>
        <vm:HomeViewModel />
    </UserControl.DataContext>

    <Grid Background="#121212">
        <StackPanel Margin="20">
            <TextBlock Text="Welkom bij Car Marketplace" 
                       FontSize="28" 
                       Foreground="White" 
                       Margin="0,0,0,20"/>

            <ItemsControl ItemsSource="{Binding FeaturedCars}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel IsItemsHost="True" />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>

                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Width="200" Margin="10" Background="#1E1E1E" CornerRadius="8">
                            <StackPanel>
                                <Image Source="{Binding ImagePath}" Height="120" Stretch="UniformToFill"/>
                                <TextBlock Text="{Binding Model}" 
                                           Foreground="White" 
                                           Margin="5,8,5,0" 
                                           FontWeight="Bold"/>
                                <TextBlock Text="{Binding Prijs, StringFormat=C}" 
                                           Foreground="#8AB6F9" 
                                           Margin="5,0,5,5"/>
                                <Button Content="Details" 
                                        Margin="5,0,5,10" 
                                        Background="#3A6EDB"
                                        Foreground="White" 
                                        Command="{Binding DataContext.ViewDetailsCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                        CommandParameter="{Binding}" />
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </StackPanel>
    </Grid>
</UserControl>
