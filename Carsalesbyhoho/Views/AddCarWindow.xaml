<Window x:Class="Carsalesbyhoho.Views.AddCarWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:Carsalesbyhoho.ViewModels"
        Title="Nieuwe Auto Toevoegen" Height="400" Width="500"
        WindowStartupLocation="CenterScreen">

    <Window.DataContext>
        <vm:AddCarViewModel />
    </Window.DataContext>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Model:" Grid.Row="0"/>
        <TextBox Grid.Row="0" Margin="60,0,0,5" Text="{Binding Model}" />

        <TextBlock Text="Prijs:" Grid.Row="1"/>
        <TextBox Grid.Row="1" Margin="60,0,0,5" Text="{Binding Prijs}" />

        <TextBlock Text="Bouwjaar:" Grid.Row="2"/>
        <TextBox Grid.Row="2" Margin="60,0,0,5" Text="{Binding Bouwjaar}" />

        <Button Grid.Row="3" Content="Kies afbeelding" Command="{Binding SelectImageCommand}" Margin="0,10,0,0" />
        <Image Grid.Row="4" Source="{Binding PreviewImage}" Height="100" Margin="0,5,0,5"/>

        <Button Grid.Row="5" Content="Toevoegen" Command="{Binding SaveCommand}" HorizontalAlignment="Right" Width="100" />
    </Grid>
</Window>
